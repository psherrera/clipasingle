# Renombrar este archivo a .env y completar los valores.
# Solo GROQ_API_KEY es opcional; si no se define, se usa Whisper local.

# (Opcional) API key de Groq para transcripción más rápida:
# https://console.groq.com/keys
GROQ_API_KEY=

# Puerto del backend (por defecto 5000)
PORT=5000
